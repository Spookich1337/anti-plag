apiVersion: v1
kind: Service
metadata:
  name: {{ include "moevm-anti-plag.fullname" . }}
  labels:
    {{- include "moevm-anti-plag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moevm-anti-plag.selectorLabels" . | nindent 4 }}
