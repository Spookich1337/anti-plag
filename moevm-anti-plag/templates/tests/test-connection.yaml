apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moevm-anti-plag.fullname" . }}-test-connection"
  labels:
    {{- include "moevm-anti-plag.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moevm-anti-plag.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
